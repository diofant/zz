TESTS = $(check_PROGRAMS)

check_LTLIBRARIES = libtests.la

libtests_la_SOURCES = tests.h utils.c
libtests_la_LIBADD = $(libtests_la_DEPENDENCIES) $(top_builddir)/libzz.la

@VALGRIND_CHECK_RULES@

check_PROGRAMS = t-binop t-memory t-misc t-pow t-str t-unop
LDADD = libtests.la $(top_builddir)/libzz.la
AM_CFLAGS=
AM_LDFLAGS=
if ENABLE_GCOV
AM_CFLAGS += -fprofile-arcs -ftest-coverage -fprofile-update=atomic
AM_LDFLAGS += -fprofile-arcs -ftest-coverage -fprofile-update=atomic
endif
AM_CFLAGS += -I$(top_srcdir)
AM_LDFLAGS += $(ZZ_LDFLAGS)
